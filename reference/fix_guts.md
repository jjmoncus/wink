# Get the guts of a banner

An internal function (used in
[`write_banners()`](https://jjmoncus.github.io/wink/reference/write_banners.md))
that facilitates printing banners to excel such that numbers are saved
as numbers and numbers are saved as text.

## Usage

``` r
fix_guts(data)
```
